# Map to add Numbers in list using  function
# same using lambda
# Squaring numbers using lambda using fun
# squaring fun using lambda
# to make the string upper char using fun
# to make the string upper char lambda
# join two list

# # Map with Built-in Function

a = [2, 3, 4, 5, 6]
b = [2, 2, 2, 2]
x = list(map(pow, a, b))
print(x)
